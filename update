#!/bin/bash
#
# BathyCat Seabed Imager - Quick Update Script
# ============================================
# 
# Simple script to update the BathyImager system from GitHub.
#
# Usage: 
#   ./update           - Interactive update with conflict resolution
#   ./update --force   - Force reset to remote version (discards local changes)
#   ./update --stash   - Auto-stash local changes and pull
#
# Author: Mike Bollinger
# Date: August 2025

set -e  # Exit on any error

# Parse command line arguments
FORCE_MODE=false
STASH_MODE=false
SKIP_DEPS=false

while [[ $# -gt 0 ]]; do
    case $1 in
        --force)
            FORCE_MODE=true
            shift
            ;;
        --stash)
            STASH_MODE=true
            shift
            ;;
        --skip-deps)
            SKIP_DEPS=true
            shift
            ;;
        --help|-h)
            echo "BathyImager Update Script"
            echo ""
            echo "Usage:"
            echo "  ./update              - Interactive update with smart dependency check"
            echo "  ./update --force      - Force reset to remote (loses local changes)"
            echo "  ./update --stash      - Auto-stash and pull"
            echo "  ./update --skip-deps  - Skip dependency check (fastest, git only)"
            echo "  ./update --help       - Show this help"
            echo ""
            exit 0
            ;;
        *)
            echo "Unknown option: $1"
            echo "Use --help for usage information"
            exit 1
            ;;
    esac
done

echo "🚀 BathyImager Seabed Imager - Quick Update"
echo "========================================"

# Check if we're in the right directory
if [ ! -f "src/main.py" ]; then
    echo "❌ Error: Not in BathyImager project directory"
    echo "   Please run this script from the project root"
    echo "   Current directory: $(pwd)"
    echo "   Looking for: src/main.py"
    echo "   Expected location: ~/BathyCat-Seabed-Imager"
    ls -la
    exit 1
fi

# Stop any running services
echo "🛑 Stopping services..."
sudo systemctl stop bathyimager 2>/dev/null && echo "   Service stopped" || echo "   No service running"

# Backup configuration
echo "💾 Backing up configuration..."
if [ -f "config/bathyimager_config.json" ]; then
    cp config/bathyimager_config.json config/bathyimager_config.json.backup
    echo "   Configuration backed up"
else
    echo "   No configuration found"
fi

# Show current version
echo "📍 Current version:"
git log --oneline -1

# Fetch and show what's new
echo "🔍 Checking for updates..."
git fetch origin
NEW_COMMITS=$(git log --oneline HEAD..origin/main | wc -l)

if [ "$NEW_COMMITS" -eq 0 ]; then
    echo "✅ Already up to date!"
    echo "   To start services: sudo systemctl start bathyimager"
    exit 0
fi

echo "📥 Found $NEW_COMMITS new updates:"
git log --oneline HEAD..origin/main

# Pull updates with conflict resolution
echo "⬇️  Pulling updates..."

# Check if we have local changes that would conflict
LOCAL_CHANGES=$(git status --porcelain 2>/dev/null)
if [ -n "$LOCAL_CHANGES" ]; then
    echo "⚠️  Local changes detected:"
    git status --short
    echo ""
    
    # Handle based on mode
    if [ "$FORCE_MODE" = true ]; then
        echo "🚨 Force mode: Discarding local changes..."
        git reset --hard origin/main
        echo "✅ Reset complete - all local changes discarded"
    elif [ "$STASH_MODE" = true ]; then
        echo "📦 Stash mode: Stashing local changes..."
        git stash push -m "Auto-stash before update $(date '+%Y-%m-%d %H:%M:%S')"
        git pull origin main
        echo "🔄 Attempting to restore local changes..."
        if git stash pop; then
            echo "✅ Local changes restored successfully"
        else
            echo "⚠️  Merge conflicts detected - please resolve manually"
            echo "   Your changes are still in git stash"
            echo "   Run: git stash list"
            echo "   Run: git stash show stash@{0}"
        fi
    else
        # Interactive mode
        echo "Update options:"
        echo "1. 🔄 Stash local changes and apply remote updates (RECOMMENDED)"
        echo "2. 🚨 Force reset to remote version (LOSE local changes)"  
        echo "3. 🛑 Cancel update"
        echo ""
        read -p "Choose option (1-3): " choice
        
        case $choice in
            1)
                echo "📦 Stashing local changes..."
                git stash push -m "Auto-stash before update $(date '+%Y-%m-%d %H:%M:%S')"
                git pull origin main
                echo "🔄 Attempting to restore local changes..."
                if git stash pop; then
                    echo "✅ Local changes restored successfully"
                else
                    echo "⚠️  Merge conflicts detected - please resolve manually"
                    echo "   Your changes are still in git stash"
                    echo "   Run: git stash list"
                    echo "   Run: git stash show stash@{0}"
                fi
                ;;
            2)
                echo "🚨 Force resetting to remote version..."
                git reset --hard origin/main
                echo "✅ Reset complete - all local changes discarded"
                ;;
            3)
                echo "🛑 Update cancelled"
                echo "   To restart services: sudo systemctl start bathyimager"
                exit 0
                ;;
            *)
                echo "❌ Invalid choice - cancelling update"
                echo "   To restart services: sudo systemctl start bathyimager"
                exit 1
                ;;
        esac
    fi
else
    # No local changes, normal pull
    git pull origin main
fi

# Update dependencies
# Check dependencies unless skipped
if [ "$SKIP_DEPS" = true ]; then
    echo "⏩ Skipping dependency check (--skip-deps)"
else
    echo "📦 Checking dependencies..."

    # Check if requirements.txt exists
    if [ ! -f "requirements.txt" ]; then
        echo "⚠️  No requirements.txt found - skipping dependency update"
    else
    if [ -d "venv" ]; then
        echo "   Using virtual environment..."
        
        # Check if venv is working
        if [ ! -f "venv/bin/activate" ]; then
            echo "⚠️  Virtual environment appears corrupted"
            echo "   Run: sudo ./scripts/install.sh --update to recreate"
        else
            # Test venv activation first
            if source venv/bin/activate 2>/dev/null; then
                echo "   Virtual environment activated successfully"
                
                # Upgrade pip and tools first
                echo "   Upgrading pip, setuptools, and wheel..."
                venv/bin/pip install --upgrade pip setuptools wheel --no-cache-dir >/dev/null 2>&1 || echo "   Warning: Could not upgrade pip tools"
                
                # Install problematic packages individually with pre-compiled wheels
                echo "   Installing dependencies (this may take a few minutes)..."
                echo "   Note: Using pre-compiled wheels to avoid compilation issues..."
                
                # Check and install only missing/outdated packages
                echo "   Checking dependencies (only installing what's needed)..."
                
                # Core packages needed for BathyImager with minimum versions
                declare -A CORE_PACKAGES=(
                    ["opencv-python"]="4.0.0"
                    ["pynmea2"]="1.0.0" 
                    ["Pillow"]="8.0.0"
                    ["piexif"]="1.0.0"
                    ["pyserial"]="3.0.0"
                    ["psutil"]="5.0.0"
                    ["schedule"]="1.0.0"
                )
                
                # Function to check if package needs installation/upgrade
                check_package_version() {
                    local package=$1
                    local min_version=$2
                    
                    # Check if package is installed and get version
                    local current_version=$(venv/bin/pip show "$package" 2>/dev/null | grep "Version:" | cut -d" " -f2)
                    
                    if [ -z "$current_version" ]; then
                        return 1  # Package not installed
                    fi
                    
                    # Simple version comparison (works for most cases)
                    local required_major=$(echo "$min_version" | cut -d. -f1)
                    local current_major=$(echo "$current_version" | cut -d. -f1)
                    
                    if [ "$current_major" -ge "$required_major" ]; then
                        return 0  # Version is adequate
                    else
                        return 1  # Version too old
                    fi
                }
                
                # Check packages and install only if needed
                INSTALL_SUCCESS=true
                PACKAGES_INSTALLED=0
                for package in "${!CORE_PACKAGES[@]}"; do
                    min_version="${CORE_PACKAGES[$package]}"
                    
                    if check_package_version "$package" "$min_version"; then
                        echo "     ✓ $package (already up-to-date)"
                    else
                        echo "     📦 Installing/updating $package..."
                        if timeout 120 venv/bin/pip install --upgrade "$package" \
                            --no-cache-dir \
                            --prefer-binary \
                            --extra-index-url https://www.piwheels.org/simple 2>/dev/null; then
                            echo "     ✓ $package installed/updated"
                            ((PACKAGES_INSTALLED++))
                        else
                            echo "     ⚠ $package failed (continuing)"
                            INSTALL_SUCCESS=false
                        fi
                    fi
                done
                
                # Handle numpy separately (check venv and system versions)
                echo "     Checking numpy..."
                VENV_NUMPY=$(venv/bin/python -c "import numpy; print(numpy.__version__)" 2>/dev/null || echo "none")
                SYSTEM_NUMPY=$(python3 -c "import numpy; print(numpy.__version__)" 2>/dev/null || echo "none")
                
                NUMPY_NEEDED=false
                if [[ "$VENV_NUMPY" == "none" ]]; then
                    if [[ "$SYSTEM_NUMPY" =~ ^[12]\. ]]; then
                        echo "     ✓ Using system numpy $SYSTEM_NUMPY (adequate)"
                    else
                        NUMPY_NEEDED=true
                    fi
                elif [[ "$VENV_NUMPY" =~ ^[12]\. ]]; then
                    echo "     ✓ Using venv numpy $VENV_NUMPY (adequate)"
                else
                    NUMPY_NEEDED=true
                fi
                
                if [ "$NUMPY_NEEDED" = true ]; then
                    echo "     📦 Installing/updating numpy..."
                    if timeout 120 venv/bin/pip install --upgrade "numpy>=1.19.0" \
                        --no-cache-dir \
                        --prefer-binary \
                        --extra-index-url https://www.piwheels.org/simple 2>/dev/null; then
                        echo "     ✓ numpy installed/updated"
                        ((PACKAGES_INSTALLED++))
                    else
                        echo "     ⚠ numpy failed (may use system version)"
                        INSTALL_SUCCESS=false
                    fi
                fi
                
                # GPIO support for Raspberry Pi
                if [[ "$OSTYPE" == "linux-gnu"* ]]; then
                    echo "     Checking GPIO support..."
                    GPIO_VERSION=$(venv/bin/pip show RPi.GPIO 2>/dev/null | grep "Version:" | cut -d" " -f2)
                    if [ -n "$GPIO_VERSION" ]; then
                        echo "     ✓ RPi.GPIO $GPIO_VERSION (already installed)"
                    else
                        echo "     📦 Installing GPIO support..."
                        if timeout 60 venv/bin/pip install RPi.GPIO \
                            --no-cache-dir --prefer-binary 2>/dev/null; then
                            echo "     ✓ RPi.GPIO installed"
                            ((PACKAGES_INSTALLED++))
                        else
                            echo "     ⚠ GPIO support failed (not critical)"
                        fi
                    fi
                fi
                
                if [ "$INSTALL_SUCCESS" = true ]; then
                    if [ "$PACKAGES_INSTALLED" -eq 0 ]; then
                        echo "   ✅ All dependencies already up-to-date (no installation needed)"
                    else
                        echo "   ✅ Dependencies updated successfully ($PACKAGES_INSTALLED packages installed/updated)"
                    fi
                else
                    echo "   ⚠️  Some packages failed but core functionality should work"
                fi
                
                deactivate 2>/dev/null || true
            else
                echo "   ⚠️  Could not activate virtual environment"
                echo "   Run: sudo ./scripts/install.sh --update to fix"
            fi
        fi
    else
        echo "   No virtual environment found - using system pip..."
        echo "   Checking system dependencies (only installing what's needed)..."
        
        # Function to check if system package is installed
        check_system_package() {
            python3 -c "import $1" 2>/dev/null && return 0 || return 1
        }
        
        # Check pip tools first
        echo "     Checking pip tools..."
        PIP_VERSION=$(pip3 --version 2>/dev/null | grep -o "[0-9]\+\.[0-9]\+" | head -1)
        if [[ "$PIP_VERSION" =~ ^[0-9]+\.[0-9]+$ ]] && [ "${PIP_VERSION%%.*}" -ge 20 ]; then
            echo "     ✓ pip $PIP_VERSION (adequate)"
        else
            echo "     📦 Upgrading pip tools..."
            pip3 install --upgrade pip setuptools wheel --user --no-cache-dir >/dev/null 2>&1 || echo "   Warning: Could not upgrade pip"
        fi
        
        # Core packages for system Python
        declare -A SYSTEM_PACKAGES=(
            ["cv2"]="opencv-python"
            ["pynmea2"]="pynmea2"
            ["PIL"]="Pillow"
            ["piexif"]="piexif"
            ["serial"]="pyserial"
            ["psutil"]="psutil"
            ["schedule"]="schedule"
        )
        
        INSTALL_SUCCESS=true
        PACKAGES_INSTALLED=0
        for import_name in "${!SYSTEM_PACKAGES[@]}"; do
            package_name="${SYSTEM_PACKAGES[$import_name]}"
            
            if check_system_package "$import_name"; then
                echo "     ✓ $package_name (already available)"
            else
                echo "     📦 Installing $package_name..."
                if timeout 120 pip3 install "$package_name" --user \
                    --no-cache-dir \
                    --prefer-binary \
                    --extra-index-url https://www.piwheels.org/simple 2>/dev/null; then
                    echo "     ✓ $package_name installed"
                    ((PACKAGES_INSTALLED++))
                else
                    echo "     ⚠ $package_name failed (continuing)"
                    INSTALL_SUCCESS=false
                fi
            fi
        done
        
        # Handle numpy for system Python
        echo "     Checking numpy..."
        if check_system_package "numpy"; then
            NUMPY_VERSION=$(python3 -c "import numpy; print(numpy.__version__)" 2>/dev/null)
            echo "     ✓ numpy $NUMPY_VERSION (already available)"
        else
            echo "     📦 Installing numpy..."
            if timeout 120 pip3 install "numpy>=1.19.0" --user \
                --no-cache-dir \
                --prefer-binary \
                --extra-index-url https://www.piwheels.org/simple 2>/dev/null; then
                echo "     ✓ numpy installed"
                ((PACKAGES_INSTALLED++))
            else
                echo "     ⚠ numpy failed"
                INSTALL_SUCCESS=false
            fi
        fi
        
        if [ "$INSTALL_SUCCESS" = true ]; then
            if [ "$PACKAGES_INSTALLED" -eq 0 ]; then
                echo "   ✅ All dependencies already available (no installation needed)"
            else
                echo "   ✅ Dependencies updated successfully ($PACKAGES_INSTALLED packages installed)"
            fi
        else
            echo "   ⚠️  Some packages failed but core functionality should work"
        fi
    fi
    fi
fi

# Restore configuration
echo "🔧 Restoring configuration..."
if [ -f "config/bathyimager_config.json.backup" ]; then
    cp config/bathyimager_config.json.backup config/bathyimager_config.json
    echo "   Configuration restored"
fi

# Fix permissions
echo "🔒 Fixing permissions..."
chmod +x scripts/*.sh 2>/dev/null || echo "   Warning: Could not set permissions on scripts"
chmod +x update
chmod +x run_bathyimager.sh 2>/dev/null || echo "   Warning: run_bathyimager.sh not found"
# Ensure Python files are readable
chmod +r src/*.py 2>/dev/null || true
chmod +r config/*.json 2>/dev/null || true

# Show new version
echo "🎉 Updated to:"
git log --oneline -1

# Test the update
echo "🧪 Testing update..."
if [ -d "venv" ] && [ -f "venv/bin/activate" ]; then
    echo "   Testing with virtual environment..."
    if source venv/bin/activate 2>/dev/null; then
        # Test with timeout to prevent hanging
        if timeout 30 venv/bin/python -c "import sys; sys.path.append('src'); import main; print('✅ Main app loads successfully')" 2>/dev/null; then
            echo "   ✅ App test passed"
        else
            echo "   ⚠️  App test failed or timed out - check for errors"
            echo "   Try: source venv/bin/activate && python src/main.py --help"
        fi
        deactivate 2>/dev/null || true
    else
        echo "   ⚠️  Could not activate virtual environment for testing"
    fi
else
    echo "   Testing with system Python..."
    # Test with timeout to prevent hanging
    if timeout 30 python3 -c "import sys; sys.path.append('src'); import main; print('✅ Main app loads successfully')" 2>/dev/null; then
        echo "   ✅ App test passed"
    else
        echo "   ⚠️  App test failed or timed out - check for errors"
        echo "   Try: python3 src/main.py --help"
    fi
fi

echo ""
echo "✨ Update complete!"
echo ""
echo "🔧 Service Management:"
echo "   To install/update service: sudo ./scripts/install.sh --update"
echo "   To start service:          sudo systemctl start bathyimager"
echo "   To enable auto-start:      sudo systemctl enable bathyimager"
echo "   To check status:           sudo systemctl status bathyimager"
echo "   To view logs:              sudo journalctl -u bathyimager -f"
echo ""
echo "🔧 If Virtual Environment Issues Persist:"
echo "   Full reinstall (recreates venv): sudo ./scripts/install.sh --update"
echo "   Manual venv recreation:          rm -rf venv && python3 -m venv venv"
echo "   Manual dependency install:       source venv/bin/activate && pip install -r requirements.txt"
echo ""
echo "🏃 Manual Running:"
echo "   • python3 src/main.py --config config/bathyimager_config.json"
echo "   • ./run_bathyimager.sh"
echo ""
echo "🔍 Hardware Diagnostics:"
echo "   • ./scripts/hardware_diagnostics.sh permissions"
echo "   • ./scripts/hardware_diagnostics.sh all"