# BathyCat Seabed Imager - Python Dependencies
# =============================================
#
# Installation Notes for Raspberry Pi:
# ------------------------------------
# • Use piwheels for pre-compiled wheels: --extra-index-url https://www.piwheels.org/simple
# • Prefer binary packages: --prefer-binary --only-binary=numpy,opencv-python,Pillow
# • If numpy hangs on "preparing metadata", run: ./scripts/fix_numpy_install.sh
#

# Core Runtime Dependencies
# -------------------------
opencv-python>=4.5.0          # Computer vision and camera interface (use latest)
pynmea2>=1.18.0               # NMEA GPS sentence parsing (use latest)
Pillow>=8.0.0                 # Image processing and manipulation (use latest)
piexif>=1.1.3                # EXIF metadata manipulation (use latest)
pyserial>=3.5                # Serial communication for GPS (use latest)
numpy>=1.19.0                # Numerical operations and array handling (use latest compatible)

# GPIO and Hardware (Raspberry Pi)
# --------------------------------
RPi.GPIO>=0.7.0,<1.0.0; sys_platform == "linux"  # GPIO control on Raspberry Pi

# System and File Operations
# --------------------------
pathlib2>=2.3.0; python_version < "3.4"  # Backport for older Python versions
dataclasses>=0.6; python_version < "3.7"  # Backport for older Python versions

# Development and Testing
# ----------------------
pytest>=6.0.0                 # Testing framework (use latest)
pytest-cov>=2.10.0            # Coverage reporting (use latest)
pytest-mock>=3.3.0            # Mocking utilities (use latest)
unittest-mock>=1.0.0; python_version < "3.3"  # Mock backport for older Python

# Code Quality and Linting
# ------------------------
black>=21.0.0                 # Code formatting (use latest)
flake8>=3.8.0                 # Style checking (use latest)
mypy>=0.812                   # Type checking (use latest)

# Documentation
# ------------
sphinx>=3.4.0                 # Documentation generation (use latest)
sphinx-rtd-theme>=0.5.0       # Documentation theme (use latest)

# Performance and System Monitoring
# ---------------------------------
psutil>=5.7.0                 # System monitoring and process management (use latest)
schedule>=1.1.0               # Task scheduling (use latest)

# Additional Utility Dependencies
# ------------------------------
setuptools>=45.0.0            # Package installation and building
wheel>=0.35.0                 # Binary package format
pip>=20.0.0                   # Package installer